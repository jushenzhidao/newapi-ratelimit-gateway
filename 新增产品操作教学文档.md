# 新增产品操作教学文档

> **适用对象**：运维人员、业务运营人员、接入新产品的负责人
> **前置条件**：限速网关已部署并正常运行（`curl http://网关地址:8080/health` 返回正常）
> **预计耗时**：5 分钟（不含同步等待）

---

## 0. 先搞懂两个概念

在动手之前，先花 30 秒理解系统里的两个核心概念：

| 概念 | 在哪里 | 作用 |
|------|--------|------|
| **产品** | 业务概念 | 你要上线的一个新业务，比如"智能客服Pro版" |
| **分组（group）** | NewAPI + 限速网关 | 产品在技术侧的落地质：NewAPI 用分组管理 token 归属，网关用分组名匹配限速策略 |

**一句话**：新增一个产品 = 在 NewAPI 建一个分组 + 在网关配一条限速策略 + 发 token 给客户。**两边的分组名必须一字不差**。

```
客户请求 → 网关提取 sk-xxx
              ↓
         Redis 查 keymap → 得到分组名 "product-c"
              ↓
         Redis 查 config:product-c → 得到限速策略
              ↓
         执行 5h/7d/30d 三窗口检查 → 放行或 429
```

---

## 1. admin_token 是什么

### 1.1 定义

`admin_token` 是**限速网关管理接口的访问密码**，配置在网关的 `config.yaml` 中：

```yaml
# 管理后台
admin:
  enabled: true
  auth_token: "change-me-in-production"   # ← 这个就是 admin_token
```

### 1.2 它保护什么

网关提供了 `/admin/*` 系列管理接口（新建/修改/删除限速策略、手动触发同步）。这些接口**只能由内部运维人员调用**，任何调用都必须在请求头带上正确的 token：

```bash
-H "Authorization: Bearer <你的admin_token>"
```

token 不对会返回 `401 Invalid admin token`，token 对不上就无法改动任何限速配置。

### 1.3 它和客户的 API Key（sk-xxx）的区别

| | admin_token | API Key (sk-xxx) |
|---|---|---|
| **谁用** | 内部运维/运营 | 客户/业务方 |
| **能干什么** | 管理所有限速策略（增删改查） | 调用 LLM 接口（受限速约束） |
| **配置在哪** | 网关的 `config.yaml` | NewAPI 控制台生成 |
| **泄露后果** | 任何人可随意改所有限速、下线产品 | 单个客户额度被盗用 |

**⚠️ 关键提醒：`admin_token` 绝不能泄露给客户，也绝不能用默认值上生产。**

### 1.4 如何正确设置

**第一步：生成一个强随机 token**（不要自己编，用工具生成）：

```bash
# Linux / macOS
openssl rand -hex 32

# Windows PowerShell
-join ((1..64) | ForEach-Object { '{0:x}' -f (Get-Random -Max 16) })
```

**第二步：写入 config.yaml 并重启网关**：

```yaml
admin:
  enabled: true
  auth_token: "3f8a1c9e2b7d4f6a8e0c5b3d7f9a1e4c..."   # 换成你生成的
```

```bash
docker-compose restart gateway
```

**第三步：妥善保管**。建议存入团队密码管理器（如 1Password / Bitwarden），不要明文写在聊天记录、wiki 里。

### 1.5 更多安全建议（可选增强）

- 把 `/admin/*` 路径在 Nginx/宝塔层限制为仅内网 IP 可访问，双保险
- `admin.enabled: false` 可整体关闭管理接口（关闭后只能改数据库调策略）
- 生产环境建议定期轮换 token（换完需重启网关）

---

## 2. 新增产品完整操作流程（五步）

以下以新增产品「**智能客服Pro**」（分组名定为 `kefu-pro`）为例。

### 第一步：在 NewAPI 中创建分组

1. 打开 NewAPI 管理后台（如 `http://newapi地址:3000`）
2. 进入 **系统设置 / 分组管理**（不同版本菜单名略有差异）
3. 新增分组，名称填 `kefu-pro`
4. 保存

> ⚠️ 分组名规则：建议全小写字母+连字符（如 `kefu-pro`），**不要用中文和空格**——网关和 NewAPI 按字符串精确匹配，特殊字符容易出问题。
>
> ⚠️ 记住这个名称，第二步必须用一模一样的。

### 第二步：在网关配置限速策略

在**能访问网关的机器**上执行（把 `<网关地址>`、`<admin_token>`、参数换成实际值）：

```bash
curl -X POST "http://<网关地址>:8080/admin/groups" \
  -H "Authorization: Bearer <admin_token>" \
  -H "Content-Type: application/json" \
  -d '{
    "group_name": "kefu-pro",
    "limit_5h": 200,
    "limit_7d": 2000,
    "limit_30d": 10000,
    "limit_type": "request",
    "scope": "key",
    "remark": "智能客服Pro：按请求数限速，每Key独立配额"
  }'
```

#### 参数逐个说明

| 参数 | 必填 | 说明 | 怎么定 |
|------|------|------|--------|
| `group_name` | ✅ | 分组名，**必须和 NewAPI 里建的完全一致** | 第一步定的名称 |
| `limit_5h` | ✅ | 5 小时滑动窗口内的配额上限 | 按产品售卖规格定 |
| `limit_7d` | ✅ | 7 天滑动窗口内的配额上限 | 一般是 5h 的 8~12 倍 |
| `limit_30d` | ✅ | 30 天滑动窗口内的配额上限 | 一般是 7d 的 4~6 倍 |
| `limit_type` | | `request` = 按调用次数；`token` = 按 token 用量 | 卖"次数套餐"选 request，卖"token 包"选 token |
| `scope` | | `key` = 每个 Key 独享配额；`group` = 全组共享一个池 | 一客一 Key 选 key；渠道商共享额度选 group |
| `remark` | | 备注，方便回溯 | 写产品名和策略说明 |

#### 成功返回

```json
{
  "id": 4,
  "group_name": "kefu-pro",
  "limit_5h": 200,
  "limit_7d": 2000,
  "limit_30d": 10000,
  "limit_type": "request",
  "scope": "key",
  "status": 1,
  "remark": "智能客服Pro：按请求数限速，每Key独立配额",
  "created_at": "2026-08-18 14:30:00",
  "updated_at": "2026-08-18 14:30:00"
}
```

> ✅ 创建成功即**立即生效**（配置已直接写入 Redis），不需要等同步、不需要重启。

#### 关于窗口设计的提醒

三个窗口是**同时生效、取最严**的。比如 5h/7d/30d = 200/2000/10000，意味着客户既不能在任意 5 小时内超 200 次，也不能在任意 7 天内超 2000 次。配置时要保证三个数在时间比例上自洽（30d 上限不能小于 7d，否则 7d 形同虚设的反例要避免）。

### 第三步：在 NewAPI 创建 token 并挂到分组

1. NewAPI 管理后台 → **令牌 / Token 管理** → 添加令牌
2. 分组选择 `kefu-pro`
3. 生成后复制 `sk-xxx` 发给客户

### 第四步：等待同步生效（或手动触发）

网关后台默认每 **60 秒**自动从 NewAPI 数据库同步一次 Key→分组 映射。

不想等 60 秒就手动触发：

```bash
curl -X POST "http://<网关地址>:8080/admin/sync" \
  -H "Authorization: Bearer <admin_token>"
```

返回 `{"message": "Sync triggered"}` 即成功。

### 第五步：验证

**用客户的 key 发一个测试请求**（base_url 指向网关）：

```bash
curl http://<网关地址>:8080/v1/chat/completions \
  -H "Authorization: Bearer sk-xxx" \
  -H "Content-Type: application/json" \
  -d '{"model": "gpt-4o-mini", "messages": [{"role": "user", "content": "hello"}]}'
```

- 返回 200 + 正常回复 → ✅ 全链路通了
- 再查一下用量是否被记录：

```bash
# 查看该分组策略和剩余配额
curl "http://<网关地址>:8080/admin/groups/kefu-pro" \
  -H "Authorization: Bearer <admin_token>"
```

- 客户接入只需改一个参数：

```python
from openai import OpenAI

client = OpenAI(
    api_key="sk-xxx",                    # NewAPI 发的 key，不变
    base_url="http://<网关地址>:8080/v1"  # 唯一改动：指向网关
)
```

---

## 3. 日常运维操作速查

| 场景 | 操作 |
|------|------|
| **调整配额**（如 5h 从 200 提到 500） | `PUT /admin/groups/kefu-pro`，body 传 `{"limit_5h": 500}`，立即生效 |
| **查看所有产品策略** | `GET /admin/groups` |
| **查看单个产品** | `GET /admin/groups/kefu-pro` |
| **临时下线产品限速** | `DELETE /admin/groups/kefu-pro`（软删除，清掉 Redis 配置，请求转为不限速直通） |
| **重新启用** | `PUT /admin/groups/kefu-pro`，body 传 `{"status": 1}` |
| **修改限速模式/粒度** | `PUT /admin/groups/kefu-pro`，传 `{"limit_type": "token"}` 或 `{"scope": "group"}` |

调整配额完整示例：

```bash
curl -X PUT "http://<网关地址>:8080/admin/groups/kefu-pro" \
  -H "Authorization: Bearer <admin_token>" \
  -H "Content-Type: application/json" \
  -d '{"limit_5h": 500, "remark": "8-18 提价：5h 200→500"}'
```

---

## 4. 常见问题排查

### Q1：创建分组时返回 400 / Create failed

最常见原因是 **group_name 重复**（分组名唯一）。先 `GET /admin/groups` 查一下是否已存在，存在就用 `PUT` 更新而不是重复创建。

### Q2：配了策略但限速不生效

按顺序检查：

1. **分组名是否两边一致**：NewAPI 的分组名 vs 网关的 `group_name`，逐字符对比（注意大小写、空格、中文全角/半角）
2. **token 是否真的挂在新分组**：NewAPI 后台确认该 token 的分组属性
3. **同步是否完成**：手动 `POST /admin/sync` 后再试
4. **策略 status 是否为 1**：`GET /admin/groups/kefu-pro` 看 `status` 字段

### Q3：客户报 429 怎么确认

429 响应体里带有信息，例如：

```json
{
  "error": "rate_limit_exceeded",
  "message": "Rate limit exceeded: 5h",
  "remaining": {"5h": 0, "7d": 834, "30d": 6234},
  "group": "kefu-pro"
}
```

`message` 告诉你触顶的是哪个窗口，`remaining` 是各窗口剩余量。业务侧决定是提额还是让客户等窗口滑动。

### Q4：客户请求返回 401 Invalid admin token

客户把 **admin_token** 当成 API Key 用了。客户的请求应该带 `sk-xxx`（NewAPI 发的），只有运维调 `/admin/*` 接口才用 admin_token。

### Q5：忘了 admin_token

登录网关所在服务器，查看 `config.yaml` 的 `admin.auth_token` 字段即可找回。

### Q6：删除分组后客户请求还能通？

是的，这是设计行为。`DELETE` 只移除限速策略（软删除 + 清 Redis），请求会按 `on_config_not_found: passthrough` 策略直通 NewAPI，由 NewAPI 自身额度体系兜底。**下线产品 ≠ 停止服务**，要停服务需在 NewAPI 侧禁用 token。

---

## 5. 操作检查清单（可直接复制使用）

```markdown
- [ ] 1. NewAPI 后台已创建分组：______（记录分组名）
- [ ] 2. 分组名已确认为纯小写字母/连字符，无中文空格
- [ ] 3. 已 POST /admin/groups 创建限速策略，返回 200
- [ ] 4. group_name 与 NewAPI 分组名逐字一致（复制粘贴核对）
- [ ] 5. 三个窗口配额（5h/7d/30d）已按售卖规格填写且数值自洽
- [ ] 6. 已在 NewAPI 创建 token 并挂到新分组
- [ ] 7. 已手动触发同步或等待 60 秒
- [ ] 8. 测试请求返回 200
- [ ] 9. GET /admin/groups/<名称> 确认剩余配额在扣减
- [ ] 10. admin_token 未出现在任何发给客户的材料中
```

---

## 附：本文涉及的接口汇总

| 方法 | 路径 | 用途 |
|------|------|------|
| POST | `/admin/groups` | 创建限速策略（产品上线） |
| GET | `/admin/groups` | 列出所有策略 |
| GET | `/admin/groups/{name}` | 查看单个策略详情 |
| PUT | `/admin/groups/{name}` | 修改策略（调额/改模式/启停） |
| DELETE | `/admin/groups/{name}` | 下线限速策略（软删除） |
| POST | `/admin/sync` | 手动触发 Key→分组 同步 |
| GET | `/health` | 网关健康检查（无需鉴权） |

> 所有 `/admin/*` 接口均需 `Authorization: Bearer <admin_token>` 请求头。
