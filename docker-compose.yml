services:
  gateway:
    build: .
    container_name: ratelimit-gateway
    ports:
      - "8080:8080"
    # 全部配置来自 .env（Redis / MySQL 为外部服务，本项目不部署）
    env_file:
      - .env
    restart: unless-stopped
